{
  "captured_at": "2026-09-25",
  "service_id": 30,
  "version_id": 383,
  "published_api_count": 16,
  "implemented_api_count": 16,
  "blocked": [],
  "fixtures_unavailable": [],
  "example_normalisations": [
    {
      "api_id": "22669",
      "section": "请求体body示例",
      "normalisation": "Removed trailing commas; values unchanged."
    },
    {
      "api_id": "22672",
      "field": "groupByonDay",
      "normalisation": "Converted the quoted integer example to its documented Integer type; value unchanged."
    },
    {
      "api_id": "22670",
      "field": "groupByonDay",
      "normalisation": "Converted the quoted integer example to its documented Integer type; value unchanged."
    },
    {
      "api_id": "22668",
      "field": "groupByonDay",
      "normalisation": "Converted the quoted integer example to its documented Integer type; value unchanged."
    }
  ],
  "waiter_gaps": [],
  "version_ids": [
    30,
    383
  ],
  "endpoint_evidence": "Endpoint table in published service 30 version 30; version 383 retains the same /acct/acct-openapi/ API family but has an empty endpoint table.",
  "response_shape_notes": [
    {
      "api_id": "22672",
      "note": "Use the complete captured success response result.result path; the response table incorrectly names the outer wrapper returnObj."
    },
    {
      "api_id": "22671",
      "note": "Use the complete captured success response result.result path; the response table incorrectly names the outer wrapper returnObj."
    },
    {
      "api_id": "22670",
      "note": "Use the complete captured success response result.result path; the response table incorrectly names the outer wrapper returnObj."
    },
    {
      "api_id": "22669",
      "note": "Use the complete captured success response result.result path; the response table incorrectly names the outer wrapper returnObj."
    },
    {
      "api_id": "22668",
      "note": "Use the complete captured success response result.result path; the response table incorrectly names the outer wrapper returnObj."
    }
  ],
  "waiter_review": {
    "waiter_count": 0,
    "notes": [
      "Billing queries are synchronous retrievals and do not expose resource lifecycle completion states."
    ]
  }
}
