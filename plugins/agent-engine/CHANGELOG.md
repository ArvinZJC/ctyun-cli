# Changelog

## Unreleased

### Added

- Add 11 commands from the published Agent Engine API catalog, with Chinese and English help, explicit request bindings and response success checks.
- Include 11 captured response fixtures and require confirmation for state-changing operations; mutations are not retried.
- Require core `>=0.5.1 <1.0.0` for response contracts, metadata support and independent request bindings.

See `openapi-catalogs/agent-engine/coverage.json` for the waiter assessment and upstream evidence limitations.
