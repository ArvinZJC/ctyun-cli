# Changelog

## Unreleased

### Added

- Add 39 commands from the published DDoS Protection (Edge Cloud Edition) API catalog, with Chinese and English help, explicit request bindings and response success checks.
- Include 39 captured response fixtures and require confirmation for state-changing operations; mutations are not retried.
- Require core `>=0.5.0 <1.0.0` for response contracts and metadata support.

See `openapi-catalogs/edge-ddos/coverage.json` for the waiter assessment and upstream evidence limitations.
