# Changelog

## Unreleased

### Added

- Added 106 CTyun OpenAPI commands with explicit HTTP response contracts, documentation-derived fixtures with explicit correction provenance, and localized command help and table labels.
- Added namespace-aware XML output and HTTP status/header output; request encodings follow the captured operation contracts.
- Covers all 106 captured OpenAPI operations; repaired documentation examples and companion references are recorded in the catalog inventory. Native endpoints and signing remain outside scope.
- Requires core `>=0.5.0 <1.0.0`.
